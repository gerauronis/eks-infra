{{- if .Values.core }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-core
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Chart.Name }}-core
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: /health
spec:
  type: NodePort
  selector:
    app: {{ .Chart.Name }}-core
  ports:
    - port: 80
      protocol: TCP
      targetPort: 4041
{{- end }}